[project]
name = "automation-scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.6"
dependencies = [
    "anthropic>=0.86.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
